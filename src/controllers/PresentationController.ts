import { PresentationsResponse, UserResponse } from 'src/@types'

export async function getAll (): Promise<Array<PresentationsResponse>> {
  return []
}

export async function getOne (url: string): Promise<PresentationsResponse> {
  return {}
}
